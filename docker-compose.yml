services:
  database:
    image: postgres:alpine
    container_name: edna-database
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_DATABASE}
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    networks:
      - internal
    ports:
      - "${DB_DEV_PORT:-}" # Only exposes in devmode
    volumes:
      - psql_volume_bp:/var/lib/postgresql/data

  backend:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: edna-bar-backend
    networks:
      - internal
    env_file:
      - .env
    restart: unless-stopped
    depends_on:
      - database
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: builder
    container_name: edna-frontend
    networks:
      - internal
    env_file:
      - .env
    restart: unless-stopped

  nginx:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: runner
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - ./nginx/conf.d/certs:/etc/ssl/cloudflare:ro
    ports:
      - "80:80"
      - "443:443"
    networks:
      - web
      - internal
    restart: unless-stopped
    depends_on:
      - frontend
      - backend

  migrate:
    image: migrate/migrate
    volumes:
      - ./migrations:/migrations
      - ./migrate.sh:/migrate.s
    entrypoint: ["sh", "-c", "/migrate.sh up"]
    depends_on:
      - database
    networks:
      - internal
    profiles:
      - migrate
    restart: "no"

volumes:
  psql_volume_bp:

networks:
  web:
    driver: bridge
  internal:
    driver: bridge
